name: Build Kebabuntu ISO

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Build Kebabuntu
      run: |
        docker run --privileged --rm -v $(pwd):/workspace -w /workspace ubuntu:jammy bash -c "
          set -e
          
          apt-get update
          apt-get install -y live-build debootstrap
          
          mkdir -p kebabuntu-build
          cd kebabuntu-build
          
          UBUNTU_MIRROR='http://archive.ubuntu.com/ubuntu/'
          
          lb config \
            --mode debian \
            --parent-distribution jammy \
            --distribution jammy \
            --parent-mirror-bootstrap \"\$UBUNTU_MIRROR\" \
            --mirror-bootstrap \"\$UBUNTU_MIRROR\" \
            --parent-mirror-binary \"\$UBUNTU_MIRROR\" \
            --mirror-binary \"\$UBUNTU_MIRROR\" \
            --architecture amd64 \
            --archive-areas 'main universe multiverse restricted' \
            --parent-archive-areas 'main universe multiverse restricted' \
            --debian-installer false \
            --bootappend-live 'boot=live components quiet splash' \
            --iso-publisher 'Kebabuntu' \
            --iso-volume 'Kebabuntu Live' \
            --iso-application 'Kebabuntu OS'
          
          # Remove security and backports from chroot archives config
          # This prevents the broken security.debian.org line from being added
          sed -i 's/^LB_CHROOT_ARCHIVES_SECURITY=.*/LB_CHROOT_ARCHIVES_SECURITY=\"false\"/' config/chroot_archives 2>/dev/null || true
          sed -i 's/^LB_CHROOT_ARCHIVES_BACKPORTS=.*/LB_CHROOT_ARCHIVES_BACKPORTS=\"false\"/' config/chroot_archives 2>/dev/null || true
          
          # Package list
          mkdir -p config/package-lists
          printf '%s\n' \
            'xfce4' \
            'xfce4-goodies' \
            'lightdm' \
            'neofetch' \
            'git' \
            'vim' \
            'htop' \
            'curl' \
            'wget' \
            'sudo' \
            'network-manager' \
            'firefox' \
            > config/package-lists/kebabuntu.list.chroot
          
          lb build --verbose
          
          if [ -f live-image-amd64.hybrid.iso ]; then
            echo '✅ ISO found!'
            cp live-image-amd64.hybrid.iso /workspace/kebabuntu.iso
            chmod 644 /workspace/kebabuntu.iso
          else
            echo '❌ ISO not found!'
            ls -la
            exit 1
          fi
        "
    
    - name: Upload ISO as Artifact
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: kebabuntu-iso
        path: kebabuntu.iso
        if-no-files-found: error
